# v0.3.0 diagnostics and release identity

Implement the contract in [the design](../design-v0.3.0.md). Five new tools, archived log recovery, rich node outputs and release identity checks ship together. The user authorized release, push and Homebrew installation; an OS-required security confirmation comes only after the actual downloaded release has passed checks.

## Status

- 2026-09-23 — Fresh Sol design reviewer approved after archive routing, aggregate bounds, encoding/redirects, Kubernetes error taxonomy and exact result schemas were clarified. Execution review pending.
- 2026-09-23 — Fresh Sol execution reviewer approved after exact interface/ownership, test-first sequencing, inventory and reproducible proof commands were fixed. Candidate harness must record PID and clean up in a finally block on failure. Ready for execution.
- 2026-09-23 — Inventory reconciled: `design/design.go` and generated `gen/argo/{service,client,endpoints}.go`, `gen/mcp_argo/{sdk_server,register,adapter_server}.go` belong to A's contract; projected model inputs/outputs belong to A; existing client/service log methods belong to B; server NewArgoService bootstrap belongs to C. Integration catalog counts and README rows belong to parent. All other numeric 25 matches are unrelated values. Release workflow/GoReleaser flags/Makefile belong to the C verifier and parent generation checks. The concrete ArgoService is the only service implementation; no separate mock implementation needs new methods.
- 2026-09-23 — Parent transport regression ran before runtime changes: stdio, HTTP and stateless HTTP all failed on old 0.1.0 identity; new tool dispatch failed as unknown. Evidence: `/tmp/go-argo-mcp-v0.3.0/integration-red.txt`. Audit content regression passed immediately because the existing result-summary whitelist already omits diagnostic contents; no audit behavior change needed.
- 2026-09-23 — Agent thread limit permits two Sol implementation slices concurrently. A and B started; C will follow on a free Sol slot. Ownership and review gates remain unchanged.

- 2026-09-23 — All three Sol implementation slices completed. Fresh Sol reviews approved after fixes for typed error preservation, parent cancellation, pod-selector fallback, UTF-8 validation, artifact cursor bounds, Kubernetes whole-operation deadlines, event namespace isolation, typed-nil configuration, and pod identity validation. Regressions cover these cases. Initial shared-interface compile failures were recorded separately from behavioral failures; the parent transport regression demonstrated old identity and unknown-tool failures before implementation.
- 2026-09-23 — Full `prek run --all-files` passed, including race tests. Dead-code and duplication findings were removed, and lint findings were corrected without gate exceptions. Regeneration preserves all 10 generated-file hashes. Release workflow syntax and six-platform server/verifier builds passed. The verifier rejects the old 0.2.1 binary's advertised 0.1.0 MCP identity.
- 2026-09-23 — Live candidate checks recovered 10,486 retained main-log lines from the original frontend failure and matched a 519-byte text artifact across five chunks against Argo directly. Absent wait logs remain absent. True archive-UID reads and running-workflow timeout are fixture-tested: the connected archive backend returned HTTP 500, and no running workflow was available. Kubernetes is not configured on this installation, so its successful diagnostics path is fixture-tested and its configuration error was checked live.

- 2026-09-23 — Final candidate rebuilt after all behavioral and lint fixes; CLI/stdio identity and the official-SDK live HTTP harness passed with MCP 0.3.0 and 30 tools. Candidate SHA-256: `b8587587b2092a44cd6b35287503267ac70dc9e49e18144af141fbc866d852af`. Full gate evidence: `/tmp/go-argo-mcp-v0.3.0/prek.log`; live evidence: `/tmp/go-argo-mcp-v0.3.0/candidate-verification.json`. Publication and installed-server verification remain pending.

- 2026-09-23 — Fresh Sol final cleanup review approved with no findings; affected package tests and uncached transport integration tests passed. No outstanding review findings remain.

## Milestones

### Milestone 1: Contract and dependency inventory

Goal: Publish one generated API that independent implementation slices can use.

Acceptance Criteria

- Every implementer has recited the workflow before edits. The fresh plan reviewer has found no remaining blockers.
- The generated service has exactly five additional methods, additive legacy result fields and required legacy string payloads preserved; all 30 tools have explicit read annotations as appropriate.
- Inventory disposition is recorded in this file's status. Shared files have one owner or explicit function boundaries.

Checklist

- [x] Read this plan, design and `/Users/luca/.agents/skills/ai-testing/SKILL.md`; recite milestone order, test-first rule, targeted commands, fresh Sol review, parent commit/push/release handoff, no generated hand edits, environment-only configuration, read-only defaults and full prek gate before editing code.
- [x] Run `rg -n 'GetWorkflowLogs|WorkflowNode|GetWorkflowArtifacts|NewArgoService|25|resultSummary|MCP\(|ldflags|Release binaries' design internal cmd gen README.md .github/workflows/release.yml .goreleaser.yaml Makefile`; reconcile service interface implementers (concrete ArgoService), generated DTOs/adapters, server bootstrap, 25-tool assertions, README catalog and audit summary tests. Treat numeric occurrences unrelated to catalog counts as false positives.
- [x] Slice A owns all `design/`, `api/argo/projection.json`, generated output, service/config struct additions in `internal/service/argo.go`, existing node/artifact methods in `internal/service/expanded.go`, and workflow-data types/mappers in `internal/argoapi/expanded.go`. Add the five exact tool contracts, source/default/limit fields and node outputs from the design; use required result scalars and nonnil collection results, optional result pointers only for absent values.
- [x] Slice A runs `make generate`, inspects `gen/argo/service.go` and generated SDK registration, then sends exact DTO/interface additions to slices B/C before they write service methods. No implementation stubs may remain at completion.
- [x] Slice A publishes signatures only for `lookupWorkflowData(ctx context.Context, namespace, name, archiveUID string) (*argoapi.WorkflowData, error)` and client `GetArchivedWorkflowData(ctx context.Context, namespace, name, archiveUID string) (*WorkflowData, error)`; runtime mapping and identity implementation follow failing tests in Milestone 2. Slice B owns artifact URL generation/downloads, including an archive URL helper. Slice A alone adds `BuildVersion string`, `Transport string`, and `Kubernetes KubernetesDiagnostics` to service configuration. The service-owned interface is `Diagnose(ctx context.Context, namespace, name, workflowUID, podName string, limit int, continueToken string) (*genargo.WorkflowPodDiagnosticsResult, error)`. Slice C implements it; nil means unconfigured. C must not edit `internal/service/argo.go`.

### Milestone 2: Argo diagnostics and retained content

Goal: Make Argo reads expose complete useful diagnostic data with bounded costs and truthful failure reporting.

Acceptance Criteria

- Client/service tests prove spec preservation, archive identity, node output bounds, wait deadlines and context sanitization; no mutation or namespace guard regression.
- Artifact/log tests prove exact-container selection, offset/UTF-8 boundaries, declared-artifact validation, traversal and redirect rejection, bounded live and archive data, meaningful fallback and retrieval errors.
- SDK tests prove invalid input fails without upstream I/O, 30-tool parity and output redaction. Test files record behavior with real HTTP fixtures, not only mapped structs.

Checklist

- [x] Immediately after contract generation, parent writes `internal/integration/diagnostics_test.go` and audit regressions in `internal/mcpaudit/interceptor_test.go` before runtime implementation. Prove initial failures with `go test ./internal/integration -run TestDiagnostic` and `go test ./internal/mcpaudit`; missing service methods may initially be compile failures, recorded separately from behavior failures.
- [x] Slice A writes failing tests in `internal/argoapi/resource_spec_test.go`, `internal/argoapi/workflow_data_test.go`, `internal/service/resource_spec_test.go`, `internal/service/wait_test.go`, `internal/service/context_test.go` and `internal/service/diagnostics_test.go` before implementing `internal/argoapi/resource_spec.go`, `internal/service/resource_spec.go`, `internal/service/wait.go`, `internal/service/context.go` and richer workflow-data/node mapping. Extend the pinned projection only for required input/output parameter/result/exit-code fields; generate models rather than editing outputs. Expose context configuration fields for Slice C bootstrap: BuildVersion, Transport and Kubernetes client/configured state.
- [x] Slice A implements bounded spec selection using raw JSON (preserve large numeric values), archived workflow identity checks, result clipping flags, wait cancellation and bounded version lookup. Run `go test ./internal/argoapi ./internal/service` after generated method implementations are present; before then use package-scoped client tests.
- [x] Slice B writes failing tests in `internal/argoapi/artifact_content_test.go`, `internal/argoapi/logs_test.go`, `internal/service/artifact_content_test.go` and `internal/service/logs_test.go`, then implements `internal/argoapi/artifact_content.go`, bounded live logs, and `internal/service/artifact_content.go` plus `internal/service/logs.go`. Slice B alone edits/removes the existing GetWorkflowLogs function in `internal/service/argo.go` and existing client GetWorkflowLogs in `internal/argoapi/client.go`; do not rewrite entire shared files.
- [x] Slice B uses the shared workflow-data lookup, validates artifact declaration and path segments, prevents credential redirects, and implements byte pagination and exact source/container rules. Extend archive URLs without modifying Slice A's workflow mapping functions. Run `go test ./internal/argoapi ./internal/service` when interface completion permits.
- [x] Parent updates `internal/integration/mcp_test.go`, `transports_test.go`, `catalog_test.go` and `internal/integration/diagnostics_test.go` for all new-tool discovery, generated invalid/default bounds, real dispatch on all transports, and MCP version. Complete the earlier regressions in `internal/mcpaudit/interceptor_test.go` to prove spec/text/output parameters never appear in audit summaries; retain existing safe summary behavior. Run `go test ./internal/integration` and `go test ./internal/mcpaudit` immediately when all methods compile.
- [x] Parent runs `go test -race ./internal/argoapi ./internal/service ./internal/integration ./internal/mcpaudit` after slices A/B/C complete.

### Milestone 3: Kubernetes diagnostics and release gate

Goal: Add explicitly configured pod diagnostics and block publication of mismatched CLI/MCP versions.

Acceptance Criteria

- Fixture tests prove namespace authorization before I/O, pod owner-UID isolation, event isolation, bounds, cancellation, TLS/config validation and unconfigured behavior. Optional backend configuration cannot change existing Argo behavior.
- The reusable verifier rejects the current v0.2.1 binary's stale MCP identity and accepts a v0.3.0 candidate; its tests cover mismatch, timeout, process cleanup and sanitized environment.
- Release workflow calls the verifier before GoReleaser and all quality gates pass.

Checklist

- [x] Slice C writes failing tests then creates `internal/kubeapi/` for the explicitly configured bounded HTTP client. Implement projected pod/container/condition/event data and owner checks from the design. Avoid adding the large Kubernetes client dependency; use standard net/http and typed JSON structs.
- [x] Slice C implements `internal/service/pod_diagnostics.go` after Slice A's DTOs are generated. Implement the published `KubernetesDiagnostics` interface; do not edit shared service struct fields. Write startup config tests, then add env settings in `internal/server/config.go` (new helper file permitted), and wire Kubernetes, BuildVersion and Transport in `internal/server/server.go`.
- [x] Slice C writes verifier tests before `internal/releaseverify/` and thin `cmd/verify-release/`. CLI flags are `-binary` and `-version`; exact CLI output and SDK serverInfo.name/version are checked under timeouts. Sanitize child env to essential OS runtime keys plus explicit MCP stdio and audit-disabled settings. Reap child processes on all paths.
- [x] Slice C adds a pre-publication step to `.github/workflows/release.yml`: derive expected version from tag by stripping only the leading v, build candidate with CGO_ENABLED=0, -trimpath and release ldflags, run verifier, then existing GoReleaser publication. No tag parsing accepted through unsanitized shell interpolation.
- [x] Slice C runs `go test ./internal/kubeapi ./internal/releaseverify ./internal/server`, then `actionlint`; parent runs `go run ./cmd/verify-release -binary /tmp/go-argo-mcp-v0.2.1/native/go-argo-mcp -version 0.2.1` and asserts nonzero exit plus a sanitized diagnostic containing both expected 0.2.1 and actual 0.1.0, saved in `/tmp/go-argo-mcp-v0.3.0/verifier-negative.txt`.

### Milestone 4: Review and real release verification

Goal: Deliver a reviewed release and prove its installed server behavior.

Acceptance Criteria

- Fresh Sol code review has no unresolved actionable findings. Full `prek run --all-files`, generation drift checks and live candidate checks pass.
- Published six-platform assets pass checksums; downloaded native release has CLI and MCP version 0.3.0 before installation approval.
- Installed running cask binary matches the release hash, HTTP handshake is 0.3.0, and the five new tools plus log fallback work through the actual HTTP server. Kubernetes live testing limitations are reported accurately.

Checklist

- [x] Parent updates README catalog, env settings, examples and `docs/releases/v0.3.0.md` with exact limits and optional Kubernetes setup. No secret values or internal cross-project rationale in public notes.
- [x] Give a fresh Sol agent the final diff and design for code review; send fixes to the implementing Sol agent and obtain a fresh re-review. Record test-first deviations and all resolved findings in status.
- [x] Parent runs `make generate`, compares sorted SHA-256 manifests before/after generation for `gen` and `internal/argoapi/models`, `go test -race ./...`, `actionlint`, `prek run --all-files`, and `git diff --check`. Run full gates once serially; fix red gates and rerun impacted checks.
- [x] Parent creates `/tmp/go-argo-mcp-v0.3.0/verify-live.py`, a reproducible official-SDK harness invoking `/tmp/go-argo-mcp-v0.2.1/http-client` with `MCP_TEST_ENDPOINT`, comparing direct Argo bytes via `ARGO_BASE_URL`, and writing `/tmp/go-argo-mcp-v0.3.0/candidate-verification.json`. Evidence includes exit status, candidate SHA-256/version, 30-tool count and each call outcome. No credentials in evidence.
- [x] Parent builds via `CGO_ENABLED=0 go build -trimpath -ldflags "-s -w -X main.version=0.3.0" -o /tmp/go-argo-mcp-v0.3.0/candidate ./cmd/go-argo-mcp` and runs `go run ./cmd/verify-release -binary /tmp/go-argo-mcp-v0.3.0/candidate -version 0.3.0`. Candidate is under `/tmp/go-argo-mcp-v0.3.0`, runs release verifier, starts isolated read-only HTTP candidate with the existing Argo backend, then uses official SDK calls to verify 30 tools, version, node outputs, selected/full specs, completed/timeout waits, context, retained frontend main logs, absent wait logs, paged text artifacts and optional Kubernetes configuration error. Compare artifact chunks against the direct Argo endpoint. Run `python3 /tmp/go-argo-mcp-v0.3.0/verify-live.py` against `http://127.0.0.1:18080/rpc`; the launcher uses ARGO_MCP_TRANSPORT=http, ARGO_MCP_ADDR=127.0.0.1:18080, ARGO_NAMESPACE=argo-ci, audit disabled and mutation flags false. Record command exit and proof in the named evidence file.
- [ ] Parent commits reviewed files and runs `git push origin main`; find CI with `gh run list --workflow ci.yml --limit 5 --json databaseId,headSha,status,conclusion` and inspect exact pushed SHA with `gh run view <id>`. After commit, run `git diff --exit-code -- gen internal/argoapi/models` and assert no untracked generated files with `git ls-files --others --exclude-standard -- gen internal/argoapi/models`.
- [ ] Parent creates `git tag v0.3.0` and runs `git push origin v0.3.0`; inspect release run via `gh run list --workflow release.yml --limit 5 --json databaseId,headSha,status,conclusion` and `gh run view <id>`. Adapt existing `/tmp/go-argo-mcp-v0.2.1/verify-assets.py` into `/tmp/go-argo-mcp-v0.3.0/verify-assets.py` to use release v0.3.0, verify six archive SHA-256 entries, safely extract the sole native executable and save asset evidence; execute `python3 /tmp/go-argo-mcp-v0.3.0/verify-assets.py`. Run `go run ./cmd/verify-release -binary /tmp/go-argo-mcp-v0.3.0/native/go-argo-mcp -version 0.3.0` before Homebrew upgrade.
- [ ] Parent runs `brew update` then `HOMEBREW_NO_AUTO_UPDATE=1 HOMEBREW_NO_INSTALL_CLEANUP=1 brew upgrade --cask CaliLuke/tap/go-argo-mcp`. Preserve LaunchAgent settings. Request user action only when macOS actually blocks the verified binary; do not remove quarantine or disable Gatekeeper.
- [ ] Once runnable, restart `com.caliluke.go-argo-mcp`, verify launcher PID with `launchctl print gui/$(id -u)/com.caliluke.go-argo-mcp`, executable via `lsof -a -p <pid> -d txt`, hash with `shasum -a 256 /opt/homebrew/bin/go-argo-mcp`, then run the same verify-live.py harness against http://127.0.0.1:8080/rpc saving installed-verification.json, then report release link, tools and any material live-testing limitation.
