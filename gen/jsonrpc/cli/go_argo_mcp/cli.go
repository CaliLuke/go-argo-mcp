// Code generated by Loom, DO NOT EDIT.
//
// go-argo-mcp JSON-RPC client CLI support package
//
// Command:
// $ loom gen github.com/CaliLuke/go-argo-mcp/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	argoc "github.com/CaliLuke/go-argo-mcp/gen/jsonrpc/argo/client"
	loomhttp "github.com/CaliLuke/loom/http"
	loomhttpcli "github.com/CaliLuke/loom/http/cli"
	loom "github.com/CaliLuke/loom/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{"argo (list-workflows|get-workflow|get-workflow-logs|terminate-workflow|retry-workflow|list-cron-workflows|get-cron-workflow|get-cron-history|toggle-cron-suspension|list-workflow-templates|get-workflow-template|list-cluster-workflow-templates|get-cluster-workflow-template)"}
} // UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " argo list-workflows --body '{\n      \"limit\": 1502936985936354876,\n      \"namespace\": \"Impedit dolor rerum labore illo in dolor.\",\n      \"status\": \"Succeeded\"\n   }'\\n"
}

type commandLine struct {
	Argo struct {
		ListWorkflows struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"List workflows in a namespace with an optional status filter." name:"list-workflows"`
		GetWorkflow struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Get detailed information about a specific workflow." name:"get-workflow"`
		GetWorkflowLogs struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Get logs from a workflow's pods." name:"get-workflow-logs"`
		TerminateWorkflow struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Terminate a running workflow." name:"terminate-workflow"`
		RetryWorkflow struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Retry a failed workflow." name:"retry-workflow"`
		ListCronWorkflows struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"List CronWorkflows in namespace(s)." name:"list-cron-workflows"`
		GetCronWorkflow struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Get CronWorkflow details including schedule and last execution." name:"get-cron-workflow"`
		GetCronHistory struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Get execution history of a CronWorkflow." name:"get-cron-history"`
		ToggleCronSuspension struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Suspend or resume a CronWorkflow." name:"toggle-cron-suspension"`
		ListWorkflowTemplates struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"List WorkflowTemplates in namespace." name:"list-workflow-templates"`
		GetWorkflowTemplate struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Get WorkflowTemplate details." name:"get-workflow-template"`
		ListClusterWorkflowTemplates struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"List ClusterWorkflowTemplates (cluster-scoped)." name:"list-cluster-workflow-templates"`
		GetClusterWorkflowTemplate struct {
			Body string `help:"" name:"body" required:""`
		} `cmd:"" help:"Get ClusterWorkflowTemplate details." name:"get-cluster-workflow-template"`
	} `cmd:"" help:"MCP service for Argo Workflows operations." name:"argo"`
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(scheme string, host string, doer loomhttp.Doer, enc func(*http.Request) loomhttp.Encoder, dec func(*http.Response) loomhttp.Decoder, restore bool) (loom.Endpoint, any, error) {
	var (
		command                                  commandLine
		args                                     []string
		svcn                                     string
		epn                                      string
		argoListWorkflowsBodyFlag                *string
		argoGetWorkflowBodyFlag                  *string
		argoGetWorkflowLogsBodyFlag              *string
		argoTerminateWorkflowBodyFlag            *string
		argoRetryWorkflowBodyFlag                *string
		argoListCronWorkflowsBodyFlag            *string
		argoGetCronWorkflowBodyFlag              *string
		argoGetCronHistoryBodyFlag               *string
		argoToggleCronSuspensionBodyFlag         *string
		argoListWorkflowTemplatesBodyFlag        *string
		argoGetWorkflowTemplateBodyFlag          *string
		argoListClusterWorkflowTemplatesBodyFlag *string
		argoGetClusterWorkflowTemplateBodyFlag   *string
	)
	{
		args = flag.Args()
		if len(args) == 0 {
			args = os.Args[1:]
		}
		path, err := loomhttpcli.Parse(&command, os.Args[0], args)
		if err != nil {
			return nil, nil, fmt.Errorf("parse command: %w", err)
		}
		argoListWorkflowsBodyFlag = &command.Argo.ListWorkflows.Body
		argoGetWorkflowBodyFlag = &command.Argo.GetWorkflow.Body
		argoGetWorkflowLogsBodyFlag = &command.Argo.GetWorkflowLogs.Body
		argoTerminateWorkflowBodyFlag = &command.Argo.TerminateWorkflow.Body
		argoRetryWorkflowBodyFlag = &command.Argo.RetryWorkflow.Body
		argoListCronWorkflowsBodyFlag = &command.Argo.ListCronWorkflows.Body
		argoGetCronWorkflowBodyFlag = &command.Argo.GetCronWorkflow.Body
		argoGetCronHistoryBodyFlag = &command.Argo.GetCronHistory.Body
		argoToggleCronSuspensionBodyFlag = &command.Argo.ToggleCronSuspension.Body
		argoListWorkflowTemplatesBodyFlag = &command.Argo.ListWorkflowTemplates.Body
		argoGetWorkflowTemplateBodyFlag = &command.Argo.GetWorkflowTemplate.Body
		argoListClusterWorkflowTemplatesBodyFlag = &command.Argo.ListClusterWorkflowTemplates.Body
		argoGetClusterWorkflowTemplateBodyFlag = &command.Argo.GetClusterWorkflowTemplate.Body
		switch path {
		case "argo list-workflows":
			svcn = "argo"
			epn = "list-workflows"
		case "argo get-workflow":
			svcn = "argo"
			epn = "get-workflow"
		case "argo get-workflow-logs":
			svcn = "argo"
			epn = "get-workflow-logs"
		case "argo terminate-workflow":
			svcn = "argo"
			epn = "terminate-workflow"
		case "argo retry-workflow":
			svcn = "argo"
			epn = "retry-workflow"
		case "argo list-cron-workflows":
			svcn = "argo"
			epn = "list-cron-workflows"
		case "argo get-cron-workflow":
			svcn = "argo"
			epn = "get-cron-workflow"
		case "argo get-cron-history":
			svcn = "argo"
			epn = "get-cron-history"
		case "argo toggle-cron-suspension":
			svcn = "argo"
			epn = "toggle-cron-suspension"
		case "argo list-workflow-templates":
			svcn = "argo"
			epn = "list-workflow-templates"
		case "argo get-workflow-template":
			svcn = "argo"
			epn = "get-workflow-template"
		case "argo list-cluster-workflow-templates":
			svcn = "argo"
			epn = "list-cluster-workflow-templates"
		case "argo get-cluster-workflow-template":
			svcn = "argo"
			epn = "get-cluster-workflow-template"
		}
	}

	var (
		data     any
		endpoint loom.Endpoint
		err      error
	)
	{
		switch svcn {
		case "argo":
			c := argoc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list-workflows":
				endpoint = c.ListWorkflows()
				data, err = argoc.BuildListWorkflowsPayload(*argoListWorkflowsBodyFlag)
			case "get-workflow":
				endpoint = c.GetWorkflow()
				data, err = argoc.BuildGetWorkflowPayload(*argoGetWorkflowBodyFlag)
			case "get-workflow-logs":
				endpoint = c.GetWorkflowLogs()
				data, err = argoc.BuildGetWorkflowLogsPayload(*argoGetWorkflowLogsBodyFlag)
			case "terminate-workflow":
				endpoint = c.TerminateWorkflow()
				data, err = argoc.BuildTerminateWorkflowPayload(*argoTerminateWorkflowBodyFlag)
			case "retry-workflow":
				endpoint = c.RetryWorkflow()
				data, err = argoc.BuildRetryWorkflowPayload(*argoRetryWorkflowBodyFlag)
			case "list-cron-workflows":
				endpoint = c.ListCronWorkflows()
				data, err = argoc.BuildListCronWorkflowsPayload(*argoListCronWorkflowsBodyFlag)
			case "get-cron-workflow":
				endpoint = c.GetCronWorkflow()
				data, err = argoc.BuildGetCronWorkflowPayload(*argoGetCronWorkflowBodyFlag)
			case "get-cron-history":
				endpoint = c.GetCronHistory()
				data, err = argoc.BuildGetCronHistoryPayload(*argoGetCronHistoryBodyFlag)
			case "toggle-cron-suspension":
				endpoint = c.ToggleCronSuspension()
				data, err = argoc.BuildToggleCronSuspensionPayload(*argoToggleCronSuspensionBodyFlag)
			case "list-workflow-templates":
				endpoint = c.ListWorkflowTemplates()
				data, err = argoc.BuildListWorkflowTemplatesPayload(*argoListWorkflowTemplatesBodyFlag)
			case "get-workflow-template":
				endpoint = c.GetWorkflowTemplate()
				data, err = argoc.BuildGetWorkflowTemplatePayload(*argoGetWorkflowTemplateBodyFlag)
			case "list-cluster-workflow-templates":
				endpoint = c.ListClusterWorkflowTemplates()
				data, err = argoc.BuildListClusterWorkflowTemplatesPayload(*argoListClusterWorkflowTemplatesBodyFlag)
			case "get-cluster-workflow-template":
				endpoint = c.GetClusterWorkflowTemplate()
				data, err = argoc.BuildGetClusterWorkflowTemplatePayload(*argoGetClusterWorkflowTemplateBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}
	return endpoint, data, nil
} // argoUsage displays the usage of the argo command and its subcommands.
func argoUsage() {
	fmt.Fprintln(os.Stderr, "MCP service for Argo Workflows operations.")
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] argo COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, "    list-workflows: List workflows in a namespace with an optional status filter.")
	fmt.Fprintln(os.Stderr, "    get-workflow: Get detailed information about a specific workflow.")
	fmt.Fprintln(os.Stderr, "    get-workflow-logs: Get logs from a workflow's pods.")
	fmt.Fprintln(os.Stderr, "    terminate-workflow: Terminate a running workflow.")
	fmt.Fprintln(os.Stderr, "    retry-workflow: Retry a failed workflow.")
	fmt.Fprintln(os.Stderr, "    list-cron-workflows: List CronWorkflows in namespace(s).")
	fmt.Fprintln(os.Stderr, "    get-cron-workflow: Get CronWorkflow details including schedule and last execution.")
	fmt.Fprintln(os.Stderr, "    get-cron-history: Get execution history of a CronWorkflow.")
	fmt.Fprintln(os.Stderr, "    toggle-cron-suspension: Suspend or resume a CronWorkflow.")
	fmt.Fprintln(os.Stderr, "    list-workflow-templates: List WorkflowTemplates in namespace.")
	fmt.Fprintln(os.Stderr, "    get-workflow-template: Get WorkflowTemplate details.")
	fmt.Fprintln(os.Stderr, "    list-cluster-workflow-templates: List ClusterWorkflowTemplates (cluster-scoped).")
	fmt.Fprintln(os.Stderr, "    get-cluster-workflow-template: Get ClusterWorkflowTemplate details.")
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s argo COMMAND --help\n", os.Args[0])
}
func argoListWorkflowsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo list-workflows", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "List workflows in a namespace with an optional status filter.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo list-workflows --body '{\n      \"limit\": 1502936985936354876,\n      \"namespace\": \"Impedit dolor rerum labore illo in dolor.\",\n      \"status\": \"Succeeded\"\n   }'")
}
func argoGetWorkflowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo get-workflow", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Get detailed information about a specific workflow.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo get-workflow --body '{\n      \"name\": \"Aperiam dolorum qui vel fuga.\",\n      \"namespace\": \"Facere sint voluptas consectetur et.\"\n   }'")
}
func argoGetWorkflowLogsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo get-workflow-logs", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Get logs from a workflow's pods.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo get-workflow-logs --body '{\n      \"container\": \"Qui mollitia.\",\n      \"max_lines\": 5366917025751733111,\n      \"namespace\": \"Incidunt blanditiis eligendi excepturi.\",\n      \"pod_name\": \"Odio saepe.\",\n      \"search\": \"Amet voluptatem non et quas.\",\n      \"workflow_name\": \"Accusamus in distinctio aliquam nihil.\"\n   }'")
}
func argoTerminateWorkflowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo terminate-workflow", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Terminate a running workflow.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo terminate-workflow --body '{\n      \"confirmation_token\": \"Sapiente est dolorem dolorem sed hic.\",\n      \"dry_run\": false,\n      \"name\": \"Ex nam fugiat repellendus libero.\",\n      \"namespace\": \"Quia est est debitis aut ipsam facere.\",\n      \"reason\": \"Nostrum et corporis quos possimus laborum inventore.\"\n   }'")
}
func argoRetryWorkflowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo retry-workflow", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Retry a failed workflow.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo retry-workflow --body '{\n      \"name\": \"Qui pariatur at dicta.\",\n      \"namespace\": \"Velit quisquam et exercitationem et voluptatem earum.\",\n      \"restart_successful\": false\n   }'")
}
func argoListCronWorkflowsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo list-cron-workflows", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "List CronWorkflows in namespace(s).")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo list-cron-workflows --body '{\n      \"namespace\": \"Doloremque quo ab quo consequatur aliquid ut.\",\n      \"suspended\": true\n   }'")
}
func argoGetCronWorkflowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo get-cron-workflow", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Get CronWorkflow details including schedule and last execution.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo get-cron-workflow --body '{\n      \"name\": \"Accusantium delectus voluptas saepe.\",\n      \"namespace\": \"Ut repudiandae nihil ut.\"\n   }'")
}
func argoGetCronHistoryUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo get-cron-history", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Get execution history of a CronWorkflow.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo get-cron-history --body '{\n      \"limit\": 1333660187961427056,\n      \"name\": \"Eligendi dolores ullam sint dolorem quidem sed.\",\n      \"namespace\": \"Aut qui est amet dignissimos.\"\n   }'")
}
func argoToggleCronSuspensionUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo toggle-cron-suspension", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Suspend or resume a CronWorkflow.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo toggle-cron-suspension --body '{\n      \"name\": \"Id sit aliquam ipsam aspernatur aspernatur.\",\n      \"namespace\": \"Nam inventore non quod id.\",\n      \"suspend\": false\n   }'")
}
func argoListWorkflowTemplatesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo list-workflow-templates", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "List WorkflowTemplates in namespace.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo list-workflow-templates --body '{\n      \"label_selector\": \"Ratione officia laborum nostrum dolores.\",\n      \"namespace\": \"Unde vero qui reiciendis atque voluptates ullam.\"\n   }'")
}
func argoGetWorkflowTemplateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo get-workflow-template", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Get WorkflowTemplate details.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo get-workflow-template --body '{\n      \"name\": \"Vel et qui eum.\",\n      \"namespace\": \"Qui ea ut error.\"\n   }'")
}
func argoListClusterWorkflowTemplatesUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo list-cluster-workflow-templates", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "List ClusterWorkflowTemplates (cluster-scoped).")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo list-cluster-workflow-templates --body '{\n      \"label_selector\": \"Blanditiis dolor aut animi.\"\n   }'")
}
func argoGetClusterWorkflowTemplateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] argo get-cluster-workflow-template", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Get ClusterWorkflowTemplate details.")

	// Flags list
	fmt.Fprintln(os.Stderr, "    -body JSON: ")

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "argo get-cluster-workflow-template --body '{\n      \"name\": \"Voluptas et.\"\n   }'")
}
